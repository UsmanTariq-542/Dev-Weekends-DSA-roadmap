class Solution {
public:
    vector<int> getConcatenation(vector<int>& nums) {
        int size=nums.size();
        for(int i=size;i<size*2;i++){
            int num=nums[i-size];
            nums.push_back(num);
        }

        return nums;
    }
};
