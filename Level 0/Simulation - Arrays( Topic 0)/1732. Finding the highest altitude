class Solution {
public:
    int largestAltitude(vector<int>& gain) {
        int alt=0;
        int result=0;
        for(int &n:gain){
            alt+=n;
            result=max(result,alt);
        }

        return result;
    }
};
